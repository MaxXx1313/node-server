#!/usr/bin/env node

var serverStarter = require('../lib/server.js');
var meta = require(__dirname + '/../package.json');

var port = process.argv[2] || 8080;
var webrootDir = process.cwd();


console.log('Server v%s', meta.version);
serverStarter.start(webrootDir, port);
